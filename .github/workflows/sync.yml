name: Sync

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  repo-arr:
    name: Create Repository Array
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        id: create-arr
        with:
          script: |
            const result = await github.graphql(`{ 
              organization(login:"${context.payload.organization.login}") {
                repositories(first:100) {
                  nodes {
                    name
                  }
                }
              }
            }`);
            console.log(JSON.stringify(result, null, 2));
            return result.organization.repositories.nodes
              .map(repo => repo.name)
              .filter(repo => repo !== context.payload.repository.name);
          github-token: ${{ secrets.GH_TOKEN }}
    outputs:
      repos: ${{ steps.create-arr.outputs.result }}
  
  patch:
    name: Patch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: git format-patch ${{ github.event.before }} --stdout | tee revision.patch
      - uses: actions/upload-artifact@v2
        with:
          name: patch
          path: revision.patch

  sync:
    name: Sync Repository
    needs:
      - repo-arr
      - patch
    strategy:
      matrix:
        repo: ${{ fromJson(needs.repo-arr.outputs.repos) }}
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository ${{ matrix.repo }}
        uses: actions/checkout@v3
        with:
          repository: ${{ github.event.organization.login }}/${{ matrix.repo }}
          token: ${{ secrets.GH_TOKEN }}
      - run: |
          git config --global user.name 'actions-user'
          git config --global user.email 'actions-user@users.noreply.github.com'
      - run: git remote add org-repo https://github.com/${{ github.event.organization.login }}/${{ github.event.repository.name }}
      - run: git fetch org-repo
      - run: git merge --allow-unrelated-histories --squash -X theirs org-repo/main
      - run: git diff-index --quiet HEAD || git commit -am "Organization sync"
      - run: git push